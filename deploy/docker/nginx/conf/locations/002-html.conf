    ###############################################################
    # serve static files (.html) directly
    location ~* ^.+.(html)$ {
#        root ${ENV_HTML_ROOT_FOLDER};
        root /usr/share/nginx;
        try_files $uri /index.html;
    }
