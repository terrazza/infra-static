
    ###############################################################
    # serve static files (images,js) directly
    location ~* ^.+.(jpg|jpeg|gif|css|png|js|ico|txt|md|doc|pdf)$ {
        root /usr/share/nginx;
        access_log      off;
        expires 30d;
    }
