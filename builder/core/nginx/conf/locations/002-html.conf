    ###############################################################
    # serve static files (.html) directly
    location ~* ^.+.(html)$ {
        root /usr/share/nginx;
        try_files $uri /index.html;
    }
